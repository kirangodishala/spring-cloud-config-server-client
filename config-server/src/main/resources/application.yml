server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/j-sandy/spinnaker-config.git
          basedir: /tmp/cd1
  application:
    name: clouddriver