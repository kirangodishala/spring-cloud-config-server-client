server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kirangodishala/cloud-config.git
          basedir: /tmp/cd1
  application:
    name: clouddriver
